The APK now contains ARM64 native code only, which makes it much smaller. The public build requests no network permissions, and the recognition runtime's built-in telemetry is disabled. Code shrinking is enabled for release builds. No changes to app features.
